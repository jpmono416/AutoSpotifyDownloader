import test from "node:test";import assert from "node:assert/strict";import {randomBytes} from "node:crypto";import {decryptToken,encryptToken} from "../src/lib/auth/token-crypto";
test("provider tokens round-trip with AES-256-GCM",{concurrency:false},()=>{process.env.TOKEN_ENCRYPTION_KEY=randomBytes(32).toString("base64");process.env.TOKEN_ENCRYPTION_KEY_VERSION="7";const encrypted=encryptToken("access:refresh");assert.equal(encrypted.keyVersion,7);assert.notEqual(encrypted.ciphertext,"access:refresh");assert.equal(decryptToken(encrypted),"access:refresh");});
test("provider token authentication fails with the wrong key",{concurrency:false},()=>{process.env.TOKEN_ENCRYPTION_KEY=randomBytes(32).toString("base64");const encrypted=encryptToken("secret");process.env.TOKEN_ENCRYPTION_KEY=randomBytes(32).toString("base64");assert.throws(()=>decryptToken(encrypted));});
